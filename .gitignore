__pycache__/
*.pyc
*.py[cod]
.venv/
.DS_Store
dist/
.coverage
coverage.xml
.mypy_cache/
.pytest_cache/
.ruff_cache/
demo-results.json
demo-evidence.md
demo-evidence.json
demo-evidence-drift.md
gauntlet-results.json
gauntlet-evidence.md
gauntlet-evidence.json

# The documentation site is a build artifact, rendered by `make site`.
site/
node_modules/

# The real target's index is rebuilt every run from the vendored corpus
# (content-addressed chunk ids, so it is reproducible, not authoritative).
real-target/var/
